from fastapi import APIRouter

router = APIRouter()

@router.post("/generate")
async def generate_documents(data: dict):
    """
    Stub endpoint: generate PDF/ZIP package.
    """
    return {"message": "Documents generated (stub)", "files": ["invoice.pdf", "usmca.pdf"], "data": data}